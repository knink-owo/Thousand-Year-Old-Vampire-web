feat: 效果执行规则引擎增强（互斥条件/预设值/行级撤回）、UX 调整

效果执行五项调整
- 互斥条件：同条目"杀死凡人角色"存在活角色候选时，其 fallback 目标
  （如"创造凡人角色"）自动标记为不适用（规则书"如果没有可用角色，就创造"句式），
  不显示操作按钮、不参与完成计数；候选消失后自动恢复可执行
- 预设值：输入类效果打开浮层时自动填入已提取的默认值（如"获得技能：嗜血"→"嗜血"）
- 确认弹窗删除：执行效果不再弹"确认执行"（行级撤回已兜底误操作）
- 撤回/重写融合：已处理行仅保留单个"撤回"——input 类撤回后
  自动展开输入浮层并预填（equivalent 原"重写"）；修复 restoreSnapshot
  替换 state 引用覆盖面板状态的 bug（nextTick 后再展开）
- killCharacter 候选限定为"活的凡人"（规则书"杀死一个凡人角色"），
  不朽者不再是候选；相应 E2E 补全（e2e_effect4/e2e_exclusion/e2e_fusion）

UX 调整
- 骰子之判展示区移到提示卡上方（完成回合后一屏可见骰子与下一提示）
- 完成回合后页面平滑滚动回顶部

工程
- effectActions.ts 新增 fallback/defaultInputOf helper；
- store 新增 restoreSnapshot（行级撤回）替代全局 undo 栈
- 单测 30 项全绿（新增 fallback/defaultInput 2 项）