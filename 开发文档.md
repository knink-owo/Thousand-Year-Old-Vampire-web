# 开发文档 · 千年吸血鬼网页版

面向开发者的技术文档。涵盖架构、数据模型、引擎规则、目录结构、本地开发、测试与部署。

---

## 1. 项目概览

| 项 | 内容 |
|---|---|
| 类型 | 单人日记式 TRPG 的网页数字工具 |
| 前端 | Vue 3（`<script setup>`）+ TypeScript |
| 构建 | Vite 8 |
| 状态 | Pinia（setup store 风格） |
| 样式 | Tailwind CSS 4 + 自定义哥特古书风主题（`src/style.css`） |
| PWA | vite-plugin-pwa（`autoUpdate` + Workbox `generateSW`），可离线安装 |
| 测试 | Vitest + jsdom（引擎单元测试共 30 项）；Playwright（E2E 冒烟，脚本见根目录 `.tools/e2e_*.py`） |
| 存储 | 浏览器 `localStorage`（存档/历史摘要/完结快照/提示包四键），无后端 |
| 数据 | 内置提示包来自规则书中译文本（`src/data/official-pack.json`） |

**架构定位**：纯前端、本地优先。游戏数据（存档、历史、日记）只存于玩家浏览器；无账号、无服务器、无网络外发。提示包为独立数据层，可导入/导出自定义包。

---

## 2. 目录结构

```
tyov-web/
├── index.html                  # SPA 入口
├── vite.config.ts              # Vite/PWA/Tailwind/Vitest 配置（含 Pages 子路径 base）
├── package.json
└── src/
    ├── main.ts                 # 应用启动（挂载 Pinia）
    ├── App.vue                 # 视图状态机 + 全局页脚（版权信息）
    ├── style.css               # 主题 tokens + 通用组件样式（.btn/.card/.input/.parchment…）
    ├── types/game.ts           # 全部领域类型（GameState/Memory/Skill/…）
    ├── data/official-pack.json # 内置中文提示包（80 提示 · 222 条目）
    ├── engine/
    │   ├── core.ts             # 纯函数引擎（骰子移动/条目推进/替代规则/终局与经历判定/记忆槽决策）
    │   ├── core.test.ts        # 引擎单元测试（21 项）
    │   ├── effectActions.ts    # 效果执行引导：效果 → 五种处理模式（auto/select/input/manual/read）
    │   ├── effectActions.test.ts # 效果引导单测（9 项）
    │   ├── effectLabels.ts     # 效果文案（结构化 Effect → 可读一行说明）
    │   └── packLoader.ts       # 提示包 JSON → 类型化 PromptPack（含自定义包导入校验）
    ├── stores/game.ts          # Pinia：回合流程 + 五特征增删改 + 存档/历史/导入导出
    ├── views/                  # 页面级组件
    │   ├── HomeView.vue        # 首页（开始旅程/教程/历史/未竟之旅）
    │   ├── CreateView.vue      # 建卡（严格按规则书 6 步）
    │   ├── GameView.vue        # 主游戏（提示/经历/日记/骰子）
    │   ├── FinishView.vue      # 终章回顾（神器可改写结局）+ 导出日记 Markdown
    │   ├── TutorialView.vue    # 教程（依规则书原文组织，含游玩示例章节）
    │   ├── HistoryView.vue     # 历史记录列表（含"回顾"入口）
    │   └── ReviewView.vue      # 历史"回顾"：已完结旅程的全量只读快照
    └── components/
        ├── PromptCard.vue      # 提示卡（条目序号 + 效果清单）
        ├── EffectActions.vue   # 效果执行引导（五种处理模式 + 行级撤回）
        └── TraitPanel.vue      # 五特征面板（记忆/技能/资源/角色/印记/日志 tab）

仓库根目录：
├── README.md                   # 用户文档（游戏介绍/使用说明/本地安装）
├── 开发文档.md                  # 本文档
├── .github/workflows/deploy.yml# Pages 自动部署（main 推送自动构建发布）
├── .tools/                     # 开发期脚本：PDF 解析、提示包提取/校验、Playwright E2E（不进生产）
└── src-data/                   # 解析中间产物（git 忽略）：结构化提示包与人工审计记录
```

---

## 3. 核心领域模型（`src/types/game.ts`）

依据规则书（中文版 ver 1.04）建模。五种特征：

| 类型 | 要点 |
|---|---|
| `Memory` | 5 记忆槽（`memorySlots` 可被提示增减）；每段至多 3 条 `Experience`；`inDiary`（移入日记不占槽）/`stabilized`（画星恒存）/`forgotten`（被划掉）/`title`（可选命名）/`note` |
| `Experience` | 提示回答的浓缩句；`lost` 可被单独划去（如提示 51） |
| `Skill` | `checked`（勾选=已使用，仅一次）/`lost`（失去） |
| `Resource` | `fixed`（固定资源）/`lost`/`isDiary`（日记实体 ≤4 记忆）/`artifact`（神器，提示 10：失去资源时须首先失去它；结局持有时可改写结局） |
| `Character` | `immortal`（凡人/不朽者）/`dead`/`isGhost`（提示 41 幽灵归来） |
| `Mark` | `removed`/`crippled`（提示 61 失能） |

其余关键类型：

- `Effect`：提示条目的**结构化效果指令**联合类型（56 种，`gainSkill`/`loseResource`/`killAllMortals`/`dreamWorld`/`gameOver`…），由提取脚本从提示文本自动解析
- `Prompt` / `PromptEntry`：`{number, entries[]}`；每条目含 `text` 与 `effects[]`
- `PromptPack`：提示包（meta + prompts），可序列化导入导出
- `DiaryEntry`：日志游戏的日记条目（含提示引用）
- `GameLogEntry`：事件流记录
- `GameRecord`：历史摘要（首页"翻阅历史"列表用；**摘要实时更新，全量快照仅在游戏完结时另存**）
- `GameState`：完整游戏状态（单一根对象，localStorage 序列化；含 `dreamWorld` 标记——提示 48 第 3 条目踏入梦境之地）

---

## 4. 回合引擎（`src/engine/core.ts`）

全部为**纯函数**（无副作用，便于单测），规则严格对应规则书：

### 4.1 骰子移动 `rollMove(current, promptCount, rng?)`

- 掷 D10 与 D6，`delta = d10 − d6`
- 正数 → 提示编号**向后**移动 `delta` 条；负数 → **向前**；0 → 停留原位（repeats）
- 不可越过提示 1（`hitFloor` 回弹到 1）；不可越过提示总数
- `rng` 可注入以便测试

### 4.2 条目推进 `entryIndexFor` / `advancePrompt`

- 提示有第 1/2/3 条目：第 N 次触达该提示使用第 N 条
- 单条目提示（72–80 终局类）永远用条目 1
- 三条目已全部回应 → **强制跳到下一提示**（即使骰子将其移回）
- 触达次数存于 `GameState.promptVisits[promptNumber]`（**可变状态不进提示包数据**）

### 4.3 替代规则 `checkAlternative`

- 按意图检查：`checkSkill` / `loseResource` / `loseSkill` / `loseCheckedSkill` / `loseUncheckedSkill`
- 指示勾选技能但无可勾选 → 失去一项资源；指示失去资源但无资源可失 → 勾选一项技能；失去技能类意图同理可落到失去资源
- 两者皆无 → `gameOver`；`completeTurn` 在回合提交时对条目效果自动做替代检查并判定终局

### 4.4 记忆槽决策 `placeExperienceDecision`

- 经历必须立即放入某段记忆：可并入未满 3 条的既有记忆 / 有空槽则新建 / 全满则 `mustResolve`（由 UI 引导玩家遗忘或移入日记）
- 已入日记、已画星恒存、已遗忘的记忆不占记忆槽（`usedMemorySlots`）

### 4.5 终局与经历判定

- `entryCausesGameOver`：条目效果含 `gameOver` 或文本含"游戏结束" → 回合提交后直接完结
- `entrySkipsExperience`：命中规则书"不要为此提示创建经历"句式（提示 24/37/43/54 等）→ UI 自动跳过本轮经历书写与记忆放置

### 4.6 效果执行引导 `effectActions.ts`

- 把 56 种 `Effect` 归类为五种**处理模式**：`auto`（点击直接执行）/`select`（选择目标后执行）/`input`（输入表单，支持预设默认值）/`manual`（引导到对应特征面板手工操作）/`read`（纯告知，一键"已知晓"）
- `EffectActionSpec` 携带交互元数据：候选过滤 `filter`、`repeat`（需执行次数，如勾选两项技能）、`confirmText`（破坏性操作确认）、`manualTab`（面板 tab 指引）
- `fallback`：互斥替代建议（如"杀死一个凡人角色，如无可用角色则创造"——候选为空时提供替代效果）
- 定位是**引导而非强制**：只提供快捷入口与提示，不越权代替玩家执行

引擎测试共 **30 项**（`core.test.ts` 21 项 + `effectActions.test.ts` 9 项）：`npm test`。

---

## 5. 状态管理（`src/stores/game.ts`）

Pinia setup store，核心导出：

| 分组 | 函数 |
|---|---|
| 回合 | `startGame` / `completeTurn`（写经历→写日记→替代检查与终局判定→掷骰推进）/ `newGame` / `endGame`（提示 69 自愿终结） |
| 记忆 | `addExperience` / `forgetMemory` / `renameMemory` / `stabilizeMemory` / `restoreMemory` / `removeExperience` / `editExperience` / `randomLoseExperience`（提示51）/ `restoreExperience` / `changeMemorySlots` / `moveMemoryToDiary` / `memoryToSkill` |
| 技能 | `addSkill` / `checkSkill` / `uncheckSkill` / `loseSkill` / `rewriteSkill` |
| 资源 | `addResource` / `loseResource`（日记丢失连带记忆划掉）/ `degradeResource` / `retrieveResource` / `convertFixedResource` / `swapResource` / `renameResource` / `toggleArtifact`（神器标记，提示10） |
| 角色 | `addCharacter` / `killCharacter` / `ageCharacter`（岁月流逝：凡人老去）/ `reviveCharacter` / `returnGhost` / `characterToResource` / `mortalToImmortal` |
| 印记 | `addMark` / `removeMark` / `crippleMark`（提示 61 失能） |
| 特殊 | `enterDreamWorld`（提示 48 第 3 条目：清空特征、唯留一把银剑、回到提示 10） |
| 撤回 | `restoreSnapshot`（行级撤回：EffectActions 每行效果执行前保存快照，撤回仅回滚该行） |
| 存档 | `persist`（每次变更自动写 localStorage + 更新历史摘要；存储不可用时置 `storageWarning` 供顶部横幅提示） |
| 历史 | `records` / `removeRecord` / `clearRecords` / `getRecordSnapshot`（"回顾"只读快照） |
| 导入导出 | `exportGameJson` / `importGameJson` / `exportDiaryMarkdown` / `exportPackJson` / `importPackJson` |

**localStorage 键**：

| 键 | 内容 |
|---|---|
| `tyov:save:v1` | 完整 `GameState`（当前可续玩存档） |
| `tyov:history:v1` | `GameRecord[]`（摘要，上限 20） |
| `tyov:history:full:v1` | `gameId → GameState` 全量快照（**仅游戏完结时保存**，供历史"回顾"只读查看；配额不足时从最旧开始丢弃重试） |
| `tyov:pack:v1` | 玩家导入的自定义提示包（默认缺省回退内置包） |

所有持久化都经 `safeGet/safeSet/safeRemove` 安全层：隐私模式、禁用站点数据或配额写满时失败不中断游戏，仅置 `storageWarning` 供 UI 提示。

> ⚠️ 数据全部在浏览器本地，**无任何网络外发**；隐私/无痕模式下 localStorage 可能被清空——这是设计约束，不是 bug。

---

## 6. 数据管线（根目录 `.tools/`，仅开发期）

内置提示包的生产路径：

1. **`extract_pdf.py`**：PyMuPDF 将规则书 PDF 逐页抽为 UTF-8 文本（需本地安装 `pymupdf`）
2. **`parse_prompts.py`**：以文本块为单位切分 80 个提示的 1–3 条目（保留视觉段落边界），输出 `src-data/prompts_raw.json`
3. **`extract_effects.py`**：正则规则表将自然语言指令（"获得技能嗜血"等）提取为结构化 `Effect` 数组，输出 `src-data/prompts.json`；人工逐条审计记录于 `src-data/effects_audit.txt`
4. **`verify_pack.py`**：校验提示包完整性（编号连续、条目结构、效果类型合法）
5. 结果手工审校后作为 `src/data/official-pack.json` 提交（80 提示 · 222 条目）

> 版权提示：规则书 PDF 与原始提取文本在 `.gitignore` 中排除，**不进入仓库**；提交的仅是有审校的提示包数据（个人学习用途）。

---

## 7. 视图状态机（`src/App.vue`）

| 视图 | 说明 | 进入方式 |
|---|---|---|
| `home` | 首页 | 启动默认 / 顶部标题 |
| `create` | 建卡 | 首页"开始旅程"（无未完存档时）|
| `game` | 主游戏 | 建卡完成 / "继续旅程" / 历史页"继续" |
| `finish` | 终章回顾 | 游戏结束（state.finished）|
| `tutorial` | 教程 | 首页/导航"教程" |
| `history` | 历史 | 首页/导航"历史" |
| `review` | 历史"回顾" | 历史页点击**已完结**旅程（只读全量快照，可导出日记 Markdown）；未完结或快照缺失时给出提示语义 |

收敛规则：位于 `create/game/finish` 时监听 `store.state` 深变化自动切换（建卡完成 → game；finished → finish）；首页与静态页不被打断。

---

## 8. 本地开发

```bash
cd tyov-web
npm install

npm run dev          # http://localhost:5173（热更新）
npm test             # 引擎单测（vitest run）
npm run test:watch   # 监听模式
npm run build        # vue-tsc 类型检查 + vite build（含 PWA 产物）
npm run preview      # 预览生产构建
```

**E2E**：仓库根 `.tools/e2e_*.py` 为 Playwright 脚本（共 20+ 个，覆盖建卡规则、效果执行、存储、回归等场景；使用系统 Edge，`EDGE` 路径常量在脚本内），须先启动 dev server：

```bash
python .tools/e2e_smoke.py         # 完整冒烟：建卡→游玩→掷骰→持久化
python .tools/e2e_home.py          # 首页/教程/历史流
python .tools/e2e_review_smoke.py  # 历史"回顾"只读快照流
python .tools/e2e_live.py          # 线上部署冒烟（改 URL 指向你的 Pages）
```

### 开发约定

- 领域逻辑放引擎纯函数 + store；组件保持薄
- 新增提示包效果类型时同步：`types/game.ts` 联合类型 → `packLoader.ts` 映射表 → `effectActions.ts` 模式表与 `effectLabels.ts` 文案 →（可选）`TraitPanel/PromptCard` UI
- UI 文案使用"千年叙事体"（第二人称、动词按钮、意象化状态语）
- 提交前跑 `npm run build`（含类型检查）与 `npm test`

---

## 9. 部署（GitHub Pages）

`.github/workflows/deploy.yml`：推送 `main`（或手动 `workflow_dispatch`）→ `npm ci`（Node 22）→ **`npm run build -- --base=/仓库名/`**（子路径适配，含 `vue-tsc -b` 类型检查）→ `upload-pages-artifact` + `deploy-pages`。

- 页面资源路径由构建期 `--base` 决定（也支持 `BASE_PATH` 环境变量），本地模拟：`npm run build -- --base=/Thousand-Year-Old-Vampire-web/`
- 首次部署需在仓库 Settings → Pages 选择 **GitHub Actions** 为源
- 国内网络如需代理：`git -c http.proxy=http://127.0.0.1:7890 push`

---

## 10. 已知边界与路线图（构想）

- **AI 写作辅助**（LLM 灵感建议/润色）：尚未实现（无任何 AI 依赖），计划按"设置中开启 + 用户自填 API Key + OpenAI 兼容接口（可接 DeepSeek/Ollama）"的方式接入
- **存储双保险**：localStorage 之外可加 IndexedDB 备份，应对隐私模式清空场景
- **多存档槽**：同一时刻仅一份可续玩的存档；已完结旅程支持全量快照回顾（`tyov:history:full:v1`），但进行中的旅程只有摘要、不存快照——可进一步扩展为完整多存档槽
- **社区提示包**：提示包格式已稳定（PromptPack JSON），可建设分享生态
