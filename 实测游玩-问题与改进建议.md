# 实测游玩 · 问题与改进建议

> 本文档记录一次完整自动化游玩（27 回合 · 日志模式 · 从建卡到提示 75 天然终局）过程中发现的问题、体验观察与改进建议。
> 对应旅程记录见《示例旅程-柳德米拉的千年.md》，存档见《示例旅程-柳德米拉的千年存档.json》。
>
> ✅ **更新（2026-09）**：P1 全部、P2 全部、P3-2 已修复并通过 24 项自动化回归验证，详见文末「修复记录」。
> ✅ **更新（2026-09）**：P3-3 移动端建卡页底部提示对比度过低已修复。

## 一、问题清单（按严重度排序）

### P1-1 效果执行状态不持久化，刷新后可重复执行 —— ✅ 已修复

- **现象**：提示卡"效果执行"区的处理进度（`doneCount`，EffectActions.vue 内 UI 级 `ref`）不写入存档。每次刷新/重新进入游戏，已执行的效果都重新显示为"未处理"（如 `0/2`），"填写/选择"按钮重新出现。
- **实测经过**：提示 1 已通过"填写"添加技能"嗜血"并杀死了沃洛德亚（状态层正确），但刷新后效果卡仍显示 `0/2`；若玩家再次点"填写"确认，会**再次添加一个同名技能"嗜血"**（`addSkill` 无重名防护）。失去类效果虽被 store 的已失守卫拦截，但 UI 计数与提示会持续误导玩家。
- **修复**：处理进度写入 `GameState.effectProgress`（键 `提示号:条目号:效果下标` → 次数）：刷新/重进后仍显示已处理、不再出现重复执行入口；行级撤回同步回退进度；换提示按键隔离不再需要 UI 级重置（顺带修复了跨提示行级撤回可能误回滚旧状态的问题）。

### P1-2 候选为空时效果无法标记完成（allDone 死锁） —— ✅ 已修复

- **现象**：`select` 模式效果在候选为空且无 `fallback` 时（如提示 46「将任何固定资源转换为便携财宝」，但玩家没有任何固定资源），浮层只显示"没有可选的资源"，没有"跳过/无适用目标"按钮，该行永远无法完成，`allDone` 永远为 false。
- **修复**：候选为空且无替代建议时提供「无适用目标，跳过此效果」按钮，按规则跳过并计入完成。

### P1-3 "岁月流逝"（凡人老化）提醒依赖单次页面会话，刷新即丢失 —— ✅ 已修复

- **现象**：`GameView.showAging` 是 UI 级 `ref`，仅在**同一页面会话内** `moves` 跨过 4 的倍数且存在在世凡人时弹出。每次刷新后重新进入游戏，`watch` 以当前 moves 为初值不再触发。
- **实测经过**：27 回合中 `moves=4/8/12/16/20/24` 多次满足条件，但因每回合都是重新加载页面，老化提醒从未出现过一次。
- **修复**：引入 `GameState.agingAcknowledgedAt`（上次玩家响应老化时的 moves）——进入页面时若 `moves - agingAcknowledgedAt >= 4` 且有在世凡人则补弹提醒；「岁月暂缓」与「因年老而去」都会记录锚点，此后跨过 4 次回答才再次提醒。**弹出后是否执行、执行给谁仍由玩家自由裁量。**

### P2-1 满槽化解入口重复，存在误操作风险 —— ✅ 已修复

- **现象**：记忆满（`mustResolve`）时，回答面板内的"遗忘/入日记"与右侧 TraitPanel 记忆区的同名按钮同时可用，两者交互方式还不一致（回答面板用 `window.confirm`，TraitPanel 用自定义确认弹层）。
- **修复**：`mustResolve` 状态下 TraitPanel 的"★恒存/入日记/转为技能/遗忘"记忆操作锁定（禁用 + 引导提示"请先在回答面板处置"），统一由回答面板化解区引导；并保留"＋记忆槽/随机失去经历"等合法化解手段。

### P2-2 效果交互依赖原生 `window.prompt/confirm` —— ✅ 已修复

- **现象**：`memoryToSkill`、`rewriteSkill`、`swapResource`、`characterToResource`、创建日记等效果执行时使用原生 `window.prompt` 询问输入（另有若干 `confirm/alert`）。
- **修复**：新增通用组件 `PromptDialog`（输入弹层）与 `ConfirmDialog`（确认/告知弹层），全站迁移完毕（EffectActions、TraitPanel、GameView、CreateView、HomeView），风格统一、PWA/移动端可用，自动化无需再拦截原生对话框。

### P3-1 记忆放置下拉的可识别性 —— 未修复（观察项）

- **现象**：放经历时的记忆选项以其**首条经历前 26 字**作为标签；当首条经历已 `lost`（被随机划去）或内容相近时难以辨认。
- **观察**：已命名记忆会显示标题（`memoryLabel` 优先 `m.title`）。建议未命名记忆显示"记忆 #N（n/3）"或同时给出序号，降低选错概率。

### P3-2 提交回合时未提示"存在未处理效果" —— ✅ 已修复

- **现象**：效果未全部处理时，"完成这一回合"按钮仍可正常提交（设计如此：引导而非强制），但 UI 没有任何提示告知玩家。
- **修复**：「完成这一回合」按钮下方在存在未标记效果时显示"⚠ 还有 N 项效果未标记处理——可返回处理，或直接完成本回合"，数量随处理实时更新。

### P3-3 建卡页底部提示在手机端对比度过低 —— ✅ 已修复

- **现象**：手机端初始化故事时，底部"记忆槽共 5 个……"（`opacity-70`）与"请填完上方所有栏位后，才能开始这段千年"（`opacity-60`）文字过淡，与浅色纸张背景几乎融为一体；禁用态"🩸 成为黑夜的生物"按钮受全局 `.btn:disabled { opacity: 0.35 }` 影响同样近乎隐形。
- **修复**：两处提示文字透明度提升至 `opacity-90`；新增 `.btn-parchment:disabled { opacity: 0.7 }` 覆盖，禁用按钮在纸面上仍清晰可读（保持"未填完"的视觉弱化但不至于消失）。

## 二、验证通过的链路（实测确认无问题）

- 建卡 → 开始旅程 → 回合循环 → 掷骰移动（含 0 停留、回弹提示 1）→ 条目 2/3 触达推进 ✓
- 效果执行：input/select/mark/auto 四类交互、互斥淘汰（"创造凡人角色"在可杀目标存在时自动不适用）✓
- 记忆放placement：追加/新建/满槽 mustResolve（遗忘或移入日记）✓
- 记忆转技能（划掉记忆、技能入册）✓；随机失去经历 ✓；记忆槽增减 ✓
- 日志模式日记写入与历史"回顾"只读快照 ✓（含导出日记 Markdown）
- 终局（提示 75 gameOver）：自动判定、历史登记、全量快照留存 ✓
- 导入导出存档、历史摘要、localStorage 持久化（含跨会话恢复）✓

## 三、修复记录（2026-09）

本次修复涉及文件：`types/game.ts`（新增 `effectProgress`/`agingAcknowledgedAt`）、`stores/game.ts`（新增 `acknowledgeAging`）、`components/EffectActions.vue`（进度持久化/跳过按钮/弹层/进度上报）、`components/TraitPanel.vue`（满槽锁定/弹层迁移）、`views/GameView.vue`（老化持久化/弹层/未处理提示）、`views/CreateView.vue`、`views/HomeView.vue`（弹层迁移）、新增 `components/PromptDialog.vue`、`components/ConfirmDialog.vue`。

验证：30 项引擎单测通过；`vue-tsc` 类型检查与生产构建通过；**24 项 Playwright 回归**（P1-1 刷新保留/防重复、P1-2 跳过、P1-3 四回合补弹/暂缓/再提醒、P2-1 面板锁定、P2-2 弹层交互、P3-2 未处理提示与实时更新）全部通过。

## 四、附：过程的自动化说明

游玩由 Playwright（系统 Edge + 持久化 profile）驱动真实浏览器逐步完成；每个回合读取提示/效果 → 按规则处理 → 撰写经历与日记提交。全程 27 回合中除上述问题外未发现数据损坏或规则执行错误。脚本位于 `.tmp/play/`（不入库）。