# 实测游玩 · 问题与改进建议

> 本文档记录一次完整自动化游玩（27 回合 · 日志模式 · 从建卡到提示 75 天然终局）过程中发现的问题、体验观察与改进建议，供后续迭代参考。
> 对应旅程记录见《示例旅程-柳德米拉的千年.md》，存档见《示例旅程-柳德米拉的千年存档.json》。

## 一、问题清单（按严重度排序）

### P1-1 效果执行状态不持久化，刷新后可重复执行

- **现象**：提示卡"效果执行"区的处理进度（`doneCount`，EffectActions.vue 内 UI 级 `ref`）不写入存档。每次刷新/重新进入游戏，已执行的效果都重新显示为"未处理"（如 `0/2`），"填写/选择"按钮重新出现。
- **实测经过**：提示 1 已通过"填写"添加技能"嗜血"并杀死了沃洛德亚（状态层正确），但刷新后效果卡仍显示 `0/2`；若玩家再次点"填写"确认，会**再次添加一个同名技能"嗜血"**（`addSkill` 无重名防护）。失去类效果虽被 store 的已失守卫拦截，但 UI 计数与提示会持续误导玩家。
- **建议**：①按 `(提示号, 条目号)` 持久化"已处理效果"集合（可与行级撤回快照共存）；②或在渲染时对"状态已满足"的效果（如该技能已存在、目标已失去）自动显示"已处理 ✓"并计入完成度。

### P1-2 候选为空时效果无法标记完成（allDone 死锁）

- **现象**：`select` 模式效果在候选为空且无 `fallback` 时（如提示 46「将任何固定资源转换为便携财宝」，但玩家没有任何固定资源），浮层只显示"没有可选的资源"，没有"跳过/无适用目标"按钮，该行永远无法完成，`allDone` 永远为 false。
- **实测经过**：提示 46 时我无固定资源，只能略过该效果直接提交回合（系统允许），但"效果已全部处理 ✓"永远不出现，界面状态含糊。
- **建议**：候选为空且无 fallback 时，提供"无适用目标，标记完成"按钮（记录为"按规则跳过"），避免计数死锁；或直接将该效果计入 `isExcluded`（参照 killCharacter 的互斥淘汰逻辑）。

### P1-3 "岁月流逝"（凡人老化）提醒依赖单次页面会话，刷新即丢失

- **现象**：`GameView.showAging` 是 UI 级 `ref`，仅在**同一页面会话内** `moves` 跨过 4 的倍数且存在在世凡人时弹出。每次刷新后重新进入游戏，`watch` 以当前 moves 为初值不再触发。
- **实测经过**：27 回合中 `moves=4/8/12/16/20/24` 多次满足条件，但因每回合都是重新加载页面，老化提醒从未出现过一次。规则书要求"每隔四五个提示，便有一位凡人会因年老而去世"，现有实现会系统性漏掉该事件。
- **建议**：老化事件写入 `GameState`（如 `pendingAging` 计数器），进入游戏时若存在未处理老化事件则强制弹出一次；或用 store 的 `moves` 与上次弹窗记录做持久化判定。

### P2-1 满槽化解入口重复，存在误操作风险

- **现象**：记忆满（`mustResolve`）时，回答面板内的"遗忘/入日记"与右侧 TraitPanel 记忆区的同名按钮同时可用，两者交互方式还不一致（回答面板用 `window.confirm`，TraitPanel 用自定义确认弹层）。
- **实测经过**：自动化点击时曾因此误遗忘了一段不应遗忘的记忆（巴彦记忆）。
- **建议**：`mustResolve` 状态下弱化或禁用 TraitPanel 的"遗忘/入日记"入口，统一由回答面板的化解区引导；或统一两种确认交互。

### P2-2 效果交互依赖原生 `window.prompt/confirm`

- **现象**：`memoryToSkill`、`rewriteSkill`、`swapResource`、`characterToResource`、创建日记等效果执行时使用原生 `window.prompt` 询问输入。
- **实测经过**：自动化必须拦截 dialog 才能继续；真实玩家在 PWA/移动端会遇到与站点风格割裂的原生弹窗（且部分浏览器对 prompt 有限制）。
- **建议**：迁移到应用内弹层（TraitPanel 已有自定义确认层先例），与整体"千年叙事体"风格统一。

### P3-1 记忆放置下拉的可识别性

- **现象**：放经历时的记忆选项以其**首条经历前 26 字**作为标签；当首条经历已 `lost`（被随机划去）或内容相近时难以辨认。
- **观察**：已命名记忆会显示标题（`memoryLabel` 优先 `m.title`），但建卡后多数记忆无标题。建议未命名记忆显示"记忆 #N（n/3）"或同时给出序号，降低选错概率。

### P3-2 提交回合时未提示"存在未处理效果"

- **现象**：效果未全部处理时，"完成这一回合"按钮仍可正常提交（设计如此：引导而非强制），但 UI 没有任何提示告知玩家"还有 N 项效果未处理，可忽略或返回处理"。
- **建议**：完成按钮旁在 `!allDone` 时显示轻量提示（如"⚠ 还有 N 项效果未标记，可直接完成"）。

## 二、验证通过的链路（实测确认无问题）

- 建卡 → 开始旅程 → 回合循环 → 掷骰移动（含 0 停留、回弹提示 1）→ 条目 2/3 触达推进 ✓
- 效果执行：input/select/mark/auto 四类交互、互斥淘汰（"创造凡人角色"在可杀目标存在时自动不适用）✓
- 记忆放placement：追加/新建/满槽 mustResolve（遗忘或移入日记）✓
- 记忆转技能（划掉记忆、技能入册）✓；随机失去经历 ✓；记忆槽增减 ✓
- 日志模式日记写入与历史"回顾"只读快照 ✓（含导出日记 Markdown）
- 终局（提示 75 gameOver）：自动判定、历史登记、全量快照留存 ✓
- 导入导出存档、历史摘要、localStorage 持久化（含跨会话恢复）✓

## 三、附：过程的自动化说明

游玩由 Playwright（系统 Edge + 持久化 profile）驱动真实浏览器逐步完成；每个回合读取提示/效果 → 按规则处理 → 撰写经历与日记提交。全程 27 回合中除上述问题外未发现数据损坏或规则执行错误。脚本位于 `.tmp/play/`（不入库）。